# Basic Node JS Rest API.

This is a basic Node JS Rest API that uses Express.js, Sequelize, and JWT for authentication.

This exists just for pratice. I might try and port it to AWS Lambda and API Gateway for fun. 


# To run

1. ``` cd user-api ```

2. ``` npm istall ```

3. ```node server.js```